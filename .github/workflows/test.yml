name: Create Release
on:
  push:
    branches: [main]
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Check latest tag
        uses: actions/github-script@v4
        id: latest-tag
        with:
          script: |
            const a = ${{ github.event }};
            console.log(JSON.stringify(a));
